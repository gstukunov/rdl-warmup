// Default score values for speakers
export const DEFAULT_SPEAKER_SCORE = 70;

// Default judge rating scale
export const JUDGE_RATING_SCALE = {
  min: 1,
  max: 7,
};

// Score scale for games
export const SPEAKER_SCORE_SCALE = {
  min: 0,
  max: 100,
};
